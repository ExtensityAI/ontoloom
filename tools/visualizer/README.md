# ontology-hydra Visualizer

This tool can be used to used to visualize ontologies and knowledge graphs generated by ontology-hydra.

## TODOs

- very basic ui: drop an ontology gml file and visualize that (or maybe work with ontology JSON directly? consider both use cases!)
- more advanced for later: drop multiple versions of an ontology and have a diff graph you can cycle through, essentially allowing you to see what has changed
- some time later: do the same for kgs
