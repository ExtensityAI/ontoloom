# TODO

## Pending

- allow live updates (by refetching data every x seconds)
- compute global layout once then use positions for children of graph!

## Completed

- Fix graph route height so the ontology graph renders.
- Simplify run layout by extracting the run header, flattening wrappers, and removing the iter passthrough layout.
- Move run page max-width containers into content pages and restore graph full-bleed sizing.
- Add ForceAtlas2 worker layout for the ontology graph and ensure the app shell allows full-height content.
